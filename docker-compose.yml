services:
  web:
    image: x-music-app:latest
    build:
      context: .
      dockerfile: Dockerfile
    command: node server.js
    restart: unless-stopped
    env_file:
      - .env
    environment:
      X_MUSIC_TASK_RUNNER_URL: http://web:8098/api/internal/jobs/run
    ports:
      - "${PORT:-8098}:8098"
    volumes:
      - x-music-data:/app/data
    healthcheck:
      test: ["CMD-SHELL", "wget -qO- http://127.0.0.1:8098/api/health >/dev/null || exit 1"]
      interval: 30s
      timeout: 5s
      retries: 5
      start_period: 20s

  trigger-deploy:
    image: x-music-app:latest
    build:
      context: .
      dockerfile: Dockerfile
    command: npm run trigger:deploy
    profiles:
      - trigger
    env_file:
      - .env
    volumes:
      - x-music-data:/app/data

  ampcast:
    image: ghcr.io/rekkyrosso/ampcast:0.9.28
    restart: unless-stopped
    ports:
      - "${AMPCAST_PORT:-8000}:8000"

volumes:
  x-music-data:
